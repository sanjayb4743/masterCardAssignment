The current web service is to validate if the route exists.

Below is the end URI :

/connected?origin=org1&destination=dest1

Input are  passed as request parameters.
The response will be Yes if the route exists and No if the route does not exists.